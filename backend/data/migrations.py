from __future__ import annotations

"""
Legacy SQLite-only master_wines ALTER helpers lived here.

Catalog schema is now defined on SQLAlchemy `MasterWine` in models.py and
created via `Base.metadata.create_all` in database.init_db.
"""
